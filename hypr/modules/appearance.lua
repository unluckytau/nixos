-- appearance.
  hl.config({
    general = {
      gaps_in  = 10,
      gaps_out = 20,
      resize_on_border = false,
      allow_tearing = false,
      layout = master,
      border_size = 0,
    },

    animations = {
      enabled = false,
    },
  })
